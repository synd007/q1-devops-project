apiVersion: v1
kind: Service
metadata:
  name: pgadmin-service
  namespace: devops-app
spec:
  type: LoadBalancer
  selector:
    app: pgadmin
  ports:
    - port: 80
      targetPort: 80
